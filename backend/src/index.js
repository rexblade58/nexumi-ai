const express=require('express'),cors=require('cors'),app=express(); app.use(cors()); app.use(express.json()); app.get('/health',(r,s)=>s.json({ok:1})); app.post('/api/chat',(r,s)=>s.json({msg:'ready'})); app.post('/api/generate-image',(r,s)=>s.json({images:[]})); app.post('/api/analyze-image',(r,s)=>s.json({analysis:'ready'})); app.listen(process.env.PORT||3001,()=>console.log('backend running'));